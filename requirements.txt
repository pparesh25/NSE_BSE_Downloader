# NSE/BSE Data Downloader - Dependencies
# Core dependencies for the application

# Data processing and analysis
pandas>=2.0.0,<3

# Async HTTP requests
aiohttp>=3.9.0,<4

# Configuration management
pyyaml>=6.0,<7

# System monitoring
psutil>=5.9.0,<8

# Readable process identity during source-mode launches
setproctitle>=1.3.0,<2

# GUI Framework (PySide6 preferred for Nuitka)
# Source installs accept any tested minor, so users on older macOS and Linux
# releases -- which have no wheel for the newest Qt -- can still run the
# application.  Release builds additionally pin the exact tested runtime through
# requirements-build.txt, and pip resolves the intersection to that one version.
PySide6>=6.6,<6.12

# Development and Testing Dependencies
# (Install dev tools with: pip install -r requirements-dev.txt)
